package a2017m833;

import java.awt.*;
import java.util.*;

public class thecake {
/*
	public  <T extends Dish> Meun<Cake> getCakes(Meun<T>list)
	{  
		T temp;
		Meun<T>x=new Menu<Cake>();
		Iterator<T> iter = list.iterator();
		
		while(iter.hasNext())
		{
				
			if ((temp=iter.next()) instanceof Cake)
			{
				x.add((Cake)temp);
			}
		}
		
		return x;
	}
	*/
}
